/* assert example */
#include <iostream>

#include <stdio.h> /* printf */
// #include <assert.h> /* assert */

using namespace std;

int main()
{
    int N;
    cin >> N;
    int ans;
    if (N == 0)
        return 0; //[重要] break：用在跳出最近的迴圈!

    int check_num[10];
    for (int i = 0; i < 100000; i++)
    {
        for (int j = 0, tmp = i; j < 10; j++)
        {
        }
    }
    return 0;
}